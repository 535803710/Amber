FEISHU_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/your-webhook-token

# Optional. Fill this only when Feishu bot signature verification is enabled.
FEISHU_WEBHOOK_SECRET=

# 飞书多维表格“接收到 Webhook 时”自动化提供的地址和 Bearer token。
FEISHU_CHANGE_WEBHOOK_URL=
FEISHU_CHANGE_WEBHOOK_TOKEN=

# Git commit records use an independent Base workflow webhook. Token is optional.
FEISHU_COMMIT_WEBHOOK_URL=
FEISHU_COMMIT_WEBHOOK_TOKEN=

# Optional team MCP source overrides. Existing shared Base values are the defaults.
# AMBER_BASE_TOKEN=Inmhb4Vl0alBIAsvzaxcxC0Ln0d
# AMBER_AI_TABLE_ID=tblppOxOQCQkAzoY
# AMBER_COMMIT_TABLE_ID=tbl9MKpf3sAHG4tR

# Optional absolute lark-cli executable or Node entry path when it is not available in PATH.
# AMBER_LARK_CLI_PATH=C:/tools/node_modules/@larksuite/cli/scripts/run.js

# Optional. Semicolon-separated absolute directories to scan for local Git commits.
# COMMIT_RECORD_SCAN_ROOTS=D:/project;E:/work

# Optional. Full scan fallback interval in milliseconds (default 3600000 = 1 hour).
# COMMIT_RECORD_RECONCILE_INTERVAL_MS=3600000

# Optional. Repository discovery interval in milliseconds (default 60000 = 60 seconds).
# COMMIT_RECORD_DISCOVERY_INTERVAL_MS=60000

# Optional. File watcher debounce delay in milliseconds (default 750).
# COMMIT_RECORD_WATCH_DEBOUNCE_MS=750

# Optional. File watcher maximum wait in milliseconds (default 5000).
# COMMIT_RECORD_WATCH_MAX_WAIT_MS=5000

# Optional. Default editor when auto-detection fails: Codex or Cursor
# AMBER_EDITOR=Cursor

# Optional. Same status and message will not notify repeatedly within this window.
VIBECODING_NOTIFY_DEDUPE_SECONDS=300

# Optional health monitor overrides (milliseconds, except multiplier).
# AMBER_HEALTH_CHECK_INTERVAL_MS=30000
# AMBER_HEALTH_BASELINE_WARN_MS=1800000
# AMBER_HEALTH_PENDING_WARN_MS=60000
# AMBER_HEALTH_CRITICAL_MULTIPLIER=4
# AMBER_HEALTH_STARTUP_GRACE_MS=120000
# AMBER_HEALTH_ALERT_REPEAT_MS=3600000
