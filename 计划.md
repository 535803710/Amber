# Codex/Cursor 通知监听转发计划

## Summary

目标改为真正监听通知，而不是手动触发脚本。

最终效果：

```text
Windows Codex/Cursor 系统通知
  -> 本地 watcher 捕获
  -> status.mjs 记录 info/wait
  -> notify.mjs 发飞书
  -> 手机飞书
  -> 小米手环提醒
```

第一版验收：**Codex/Cursor 产生 Windows 系统 toast 后，自动转发到飞书/手环。**

第二版再做：**监听 Codex/Cursor 内部弹窗/确认框**。内部弹窗不是 Windows 系统通知，必须单独走 UI Automation 监听。

## Key Changes

1. **系统通知监听器 v1**
   - 新增 `scripts/watch-notifications.mjs` 作为 Node 入口。
   - 新增 `scripts/windows-notification-listener.ps1` 调用 Windows `UserNotificationListener`。
   - 默认监听 app 名称包含：
     ```text
     Codex, Cursor
     ```
   - 捕获通知后转成短消息：
     ```text
     Codex：<通知标题> - <通知正文>
     Cursor：<通知标题> - <通知正文>
     ```
   - 调用：
     ```powershell
     node scripts/status.mjs info "<消息>" --notify --force
     ```
   - 使用 `.local/watched-notifications.json` 记录已转发通知 ID，避免同一条系统通知重复转发。

2. **权限和启动流程**
   - watcher 启动时检查 Windows 通知监听权限。
   - 如果未授权，输出引导并打开/提示用户去：
     ```text
     Windows 设置 -> 隐私和安全性 -> 通知 -> 用户通知访问
     ```
   - 没有权限时不静默失败，明确提示当前不能监听。
   - 依据 Microsoft 文档，`UserNotificationListener` 需要显式用户授权。

3. **运行方式**
   - 新增 npm 命令：
     ```json
     {
       "watch:notifications": "node scripts/watch-notifications.mjs"
     }
     ```
   - 新增 Cursor task：
     ```text
     mi-notic: watch Codex/Cursor notifications
     ```
   - 默认前台运行，用户可在 Cursor 终端里停止。
   - 支持参数：
     ```powershell
     node scripts/watch-notifications.mjs --apps Codex,Cursor
     node scripts/watch-notifications.mjs --interval 2
     node scripts/watch-notifications.mjs --dry-run
     node scripts/watch-notifications.mjs --probe
     ```

4. **内部弹窗监听 v2**
   - 新增 `scripts/watch-ui-prompts.mjs`。
   - 新增 `scripts/windows-ui-prompt-listener.ps1`。
   - 使用 Windows UI Automation 轮询 Codex/Cursor 可见窗口。
   - 匹配确认类关键词：
     ```text
     confirm, approve, allow, run command, permission, 确认, 允许, 批准, 需要
     ```
   - 命中后发送：
     ```powershell
     node scripts/status.mjs wait "Codex/Cursor 需要你确认：<窗口/控件摘要>" --force
     ```
   - 只读监听，不自动点击、不自动批准。

5. **文档**
   - README 改成以 watcher 为主，而不是手动命令为主。
   - `docs/cursor.md` 增加如何启动监听 task。
   - `docs/codex.md` 明确两类能力：
     - 系统通知：v1 自动监听。
     - 内部确认框：v2 UI Automation 监听，存在误报/漏报风险。

## Test Plan

- `node scripts/watch-notifications.mjs --probe --dry-run --apps "*"`
  - 能列出当前可读取的 Windows 通知，不发飞书。
- `node scripts/watch-notifications.mjs --dry-run --apps Codex,Cursor`
  - 触发 Codex/Cursor 系统通知后，终端打印将要转发的消息。
- `node scripts/watch-notifications.mjs --apps Codex,Cursor`
  - 触发 Codex/Cursor 系统通知后，飞书/手环收到通知。
- 重复同一条系统通知：
  - `.local/watched-notifications.json` 去重，不重复转发。
- 权限未开启：
  - watcher 明确提示需要开启 Windows 用户通知访问。
- v2 验证：
  - 打开 Codex 命令确认框或 Cursor 内部确认提示。
  - `watch-ui-prompts` 捕获关键词并发送 `wait` 通知。
  - 不执行任何点击或批准动作。

## Assumptions

- 第一版只要求监听 **Windows 系统通知 toast**。
- Codex/Cursor 内部弹窗不属于系统通知，放第二阶段实现。
- 继续不新增 npm 依赖；使用 Node + PowerShell/WinRT。
- `.env` 保存飞书 webhook/secret，不提交。
- `.local/` 保存 watcher 状态和去重缓存，不提交。
- 官方参考：Microsoft `UserNotificationListener` 需要显式授权并可读取用户通知：[Notification listener - Windows apps](https://learn.microsoft.com/en-us/windows/apps/design/shell/tiles-and-notifications/notification-listener)。
